import { FiltroPorPatentePipe } from './filtro-por-patente.pipe';

describe('FiltroPorPatentePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPorPatentePipe();
    expect(pipe).toBeTruthy();
  });
});
